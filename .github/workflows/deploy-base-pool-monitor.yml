name: Verify and configure LAPTOP monitor

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "base-pool-monitor-worker/**"
      - ".github/workflows/deploy-base-pool-monitor.yml"

permissions:
  contents: read
  id-token: write

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: base-pool-monitor-worker
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --no-audit --no-fund
      - run: npm test
      - name: Securely configure the live Worker from existing Telegram secrets
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
          WORKER_URL: https://laptop-base-pool-monitor.yaoyederener.workers.dev
        run: node scripts/configure-worker.mjs
